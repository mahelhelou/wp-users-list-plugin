<?php

if ( ! defined( 'ABSPATH' ) ) exit; // Exit if accessed directly

if ( ! class_exists( 'WP_USERS_LIST' ) ) {
  class WP_USERS_LIST {
		/**
		 * This file was filled with tons of code to create options page used to filter users list table, but I founded easier way to do the same job using a third-party javascript library :)
		 */
  }
}